import { Archive, LockKeyhole, Pencil, Plus, Shapes } from 'lucide-react';
import { type FormEvent, useEffect, useState } from 'react';
import { useArchiveCategory, useCategories, useCreateCategory, useUpdateCategory } from '../api/queries';
import type { Category, CategoryInput } from '../api/types';
import { Badge, Button, Card, EmptyState, ErrorState, Field, FormError, Input, LoadingState, Modal, PageHeader, Select } from '../components/ui';
const blank:CategoryInput={name:'',kind:'expense',color:'#d06f4b',icon:null};
export default function CategoriesPage(){const query=useCategories(true);const [dialog,setDialog]=useState<{open:boolean;value:Category|null}>({open:false,value:null});return <><PageHeader eyebrow="Organization" title="Categories" description="System categories provide a stable base; your custom categories can be adapted or archived." action={<Button onClick={()=>setDialog({open:true,value:null})}><Plus className="size-4"/>New</Button>}/>{query.isPending?<LoadingState/>:query.isError?<ErrorState error={query.error} retry={()=>query.refetch()}/>:query.data.length===0?<EmptyState icon={<Shapes/>} title="No categories" description="Create a category to make your spending breakdown more useful."/>:<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{query.data.map(item=><Card key={item.id} className={`card-lift flex items-center gap-3 p-4 ${item.isArchived?'opacity-50':''}`}><span className="size-3 rounded-full shadow-sm ring-4 ring-brand/[.035]" style={{backgroundColor:item.color??'#879392'}}/><div className="min-w-0 flex-1"><p className="truncate font-semibold">{item.name}</p><div className="mt-1.5 flex flex-wrap gap-1"><Badge>{item.kind}</Badge>{item.isSystem&&<Badge tone="info">System</Badge>}{item.isArchived&&<Badge>Archived</Badge>}</div></div>{item.isSystem?<LockKeyhole className="size-4 text-muted" aria-label="Immutable system category"/>:!item.isArchived&&<button className="control-press grid size-11 place-items-center rounded-xl outline-none hover:bg-brand/[.06] focus-visible:ring-2 focus-visible:ring-accent active:scale-95" onClick={()=>setDialog({open:true,value:item})} aria-label={`Edit ${item.name}`}><Pencil className="size-4"/></button>}</Card>)}</div>}<CategoryDialog {...dialog} onClose={()=>setDialog({open:false,value:null})}/></>}
function CategoryDialog({open,value,onClose}:{open:boolean;value:Category|null;onClose:()=>void}){const [form,setForm]=useState<CategoryInput>(blank);useEffect(()=>setForm(value?{name:value.name,kind:value.kind,color:value.color,icon:value.icon}:blank),[value,open]);const create=useCreateCategory();const update=useUpdateCategory(value?.id??'');const archive=useArchiveCategory();const mutation=value?update:create;return <Modal open={open} onClose={onClose} title={value?'Edit category':'New category'}><form className="grid gap-4" onSubmit={(e:FormEvent)=>{e.preventDefault();mutation.mutate(form,{onSuccess:onClose})}}><Field label="Name"><Input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></Field><Field label="Used for"><Select value={form.kind} onChange={e=>setForm({...form,kind:e.target.value as Category['kind']})}><option value="expense">Expenses</option><option value="income">Income</option><option value="both">Both</option></Select></Field><Field label="Color"><Input type="color" value={form.color??'#d06f4b'} onChange={e=>setForm({...form,color:e.target.value})}/></Field><FormError error={mutation.error||archive.error}/><div className="flex gap-2">{value&&<Button type="button" variant="danger" onClick={()=>archive.mutate(value.id,{onSuccess:onClose})}><Archive className="size-4"/>Archive</Button>}<Button className="ml-auto" disabled={mutation.isPending}>{mutation.isPending?'Saving…':'Save category'}</Button></div></form></Modal>}
